/*double characterResponsive(
    {String? sizeState,
    double? size,
    double? currentScreenWidth,
    double? currentScreenheight}) {
  const double mainWidthScreen = 710;
  const double mainHeightScreen = 360;

  if (sizeState == "height") {
    return (currentScreenheight! / (mainHeightScreen / size!)).roundToDouble();
  } else {
    return (currentScreenWidth! / (mainWidthScreen / size!)).roundToDouble();
  }
}
*/